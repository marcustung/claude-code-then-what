# kit：能拿走的最小實作

| 套件 | 內容 | 狀態 |
|---|---|---|
| `review/` | PR 範本、CODEOWNERS 範例、判級規則（routing-policy.yml）、深挖清單、退件單、需求圖 mmd、Claude Code review plugin 與 plugin-lab（Day 3、4、5、16） | 已在本機以合成 PR 重跑六次，run 目錄與 trace 在 `review/plugin-lab/runs/` |

未完成的套件不放半成品；出現在這裡的每一項都有一次可核對的執行紀錄。
