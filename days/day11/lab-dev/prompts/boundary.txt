你是這個專案的 RD。plan.md 的第三列寫著：「查結果有沒有一路傳對——補 API 邊界測試，讓測試接收端保留必要的通知欄位；驗收：HTTP、日誌與通知的 refund_requested 對得上；依序重送不新增通知。」

Domain 層的七個情境已經全綠（見 tests/DomainTests/Program.cs）。現在要驗的是 Domain 以外的那一段。

**先不要修改任何檔案。** 讀 src/Api/Program.cs、src/Domain/Cancellation.cs、src/FakeSink/Program.cs、specs/rules-v2.md 與 plan.md，然後回答：

1. `RefundRequested` 這個值從 Domain 算出來之後，一路會經過哪些地方？每一個都附檔名與行號。
2. 要證明「外面收到的是對的值」，每一個接點各該看哪一份結果？請具體到檔名或欄位，不要只說「看日誌」。
3. 用現有的程式與 FakeSink，上面每一項是不是真的驗得出來？**如果有哪一項用現有材料驗不出來，明講是哪一項、為什麼、以及要先改什麼才驗得到。**
4. 「依序重送不新增通知」這一條，要看哪個欄位才分得出「這次真的轉換了」與「這次是重複請求」？

最後用一段話說明：哪些結論是你從程式讀到的，哪些是你推論的。推論的部分標明依據不足之處，不要用「應該」帶過。
