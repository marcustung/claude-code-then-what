你是這個專案的 RD。我們有一支 `ci.py`，依序跑三步：分流自檢、Domain 測試、API 整合驗證，任何一步失敗就停。本機跑過，三步都綠。

我現在要回答一個問題：**這三步全綠，能證明什麼？不能證明什麼？**

先不要修改任何檔案。讀 `ci.py`、`route-review.py`、`azure-pipelines.yml`、`verify-integration.py` 與 `tests/DomainTests/Program.cs`，然後回答：

1. `ci.py` 的每一步，失敗時是怎麼讓整條流程停的？附行號說明退出碼怎麼傳遞。如果有任何一步的失敗可能被吞掉，指出在哪裡。
2. 我想做一次負向演練證明閘門真的會停：**換回舊版 Domain，預期在哪一步、哪一格失敗？** 說出你預期的失敗訊息長什麼樣。
3. 有沒有哪一種失敗，會讓流程「也回非零」，但其實根本沒跑到測試？如果有，要怎麼從保存的輸出裡分辨這兩種非零？
4. `azure-pipelines.yml` 跟本機 `ci.py` 之間，有哪些東西是本機有、遠端不一定有的？逐項列出並說明會怎麼壞。

最後分開說明：哪些是你從檔案讀到的，哪些是推論。推論的部分標明依據不足之處。
