你是這個專案的 RD。我們把這個 .NET 服務做成本機發布產物（`dotnet publish`），然後**不從原始碼**、改成從發布出來的 DLL 啟動，再跑一次跟之前相同的 HTTP 驗證。

先不要修改任何檔案。讀 `package-manifest.json`、`verify-integration.py`、`src/Api/Api.csproj`、`src/Api/Program.cs` 與 `runs/package-smoke-01/report.json`，然後回答：

1. 從發布產物啟動，跟從原始碼 `dotnet run` 啟動，執行時有哪些東西可能不一樣？逐項說明，並指出這個專案裡哪些設定會受影響（附檔名與行號）。
2. `package-manifest.json` 記了每個檔的 SHA256。**這份清單能證明什麼、不能證明什麼？** 例如它能不能證明「這次 smoke 測的就是這一包」？如果不能，缺哪一步？
3. 這次 smoke 跑的檢查跟之前整合驗證的檢查是同一組嗎？如果是同一組，那這次多證明了什麼？如果只是重跑一次，請直說。
4. 有哪些「原始碼會過、產物不會過」的錯誤，是這次 smoke **驗不出來**的？舉具體例子，並說明要驗到它們還要加什麼。

最後分開說明：哪些是你從檔案讀到的，哪些是推論。推論的部分標明依據不足之處，不要用「應該」帶過。
