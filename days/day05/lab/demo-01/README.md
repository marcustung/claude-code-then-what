# demo-01：Day 5 讀者示範的那一次執行

2026-09-17 12:01，在 `fixtures/pr-A` 目錄以唯讀工具、不載入 plugin、`--output-format json` 跑一次；`run.json` 是 CLI 原始回傳，`prompt.txt` 是提示，`meta.json` 是參數與起訖。與 S-A 的差別：無 plugin、提示不同、要求三句結論。只是一次執行，不是統計。
