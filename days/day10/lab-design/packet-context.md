# 設計包界線
這是Day9開工分析的下一步。新增同一git快照的API與接收端，供確認呼叫關係；是既有程式回讀，不冒充本輪新開發。
本輪要設計的只有rules-v2.md退款要求與重複取消，不重寫API、不串付款、不接受通知提案。讀到實作不代表商業需求已核准。
不執行測試，引用只能支持靜態主張；並行操作、重啟持久性、外部接收效果都未由本輪驗證。
三種既有公開型別是Order、CancellationResult、Cancellation；不要把函式名誤認為型別。
