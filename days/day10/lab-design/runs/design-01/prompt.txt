請讀packet-context.md、manifest.json、scope-handoff.md、specs/與src/、tests/提供的檔案。只讀，不寫程式或執行測試。
這次目標是把Day9的修改範圍變成可以審查的設計及實作計畫。以rules-v2.md為本輪教學目標，通知文件仍是提案。
交付：
1. 從API取消入口追到Domain與結果的使用端，每個關係附file:line。區分呼叫與資料流，不可只列資料夾。
2. 對比把退款要求規則放API或放Domain的取捨，提出符合目前scope的最小方案與不做範圍。
3. RefundRequested欄位型別不變，是否有外部行為或資料意義改變？指出看得到與尚未知道的消費者，不把看不到說不存在。
4. 給一份短的plan.md草稿：要改的檔案、順序、需要的單元與整合情境、每個結果以誰為準、待人接受的風險。這只是設計，不能宣稱測試通過。
5. 一張簡短Mermaid元件圖、一張首次/重複取消循序圖。標明現況與待改之處，API回應、狀態更新、通知收據不可混成一件事。
請核對既有程式的並行與持久性限制，不替本輪加新需求或保證恰好一次。只回Markdown。