# 怎麼參與

- `days/*/lab/` 與 `shared/` 只增不改：每次重跑放新的 run 目錄，不覆寫既有 trace；有問題開 issue 附 run 名稱。
- `kit/` 各項有 VERSION 與 CHANGELOG；改行為就升版。
- `templates/` 可直接複製使用；修改建議請附你填過的一份。
- 這個 repo 由私有寫作 repo 的建置腳本產生（`tools/` 內只放產圖與匯出工具），直接對這裡發 PR 的文字改動會在下次建置被覆寫；請改開 issue。
